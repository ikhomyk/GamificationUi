import { CreateColorPipe } from './create-color.pipe';

describe('CreateColorPipe', () => {
  it('create an instance', () => {
    const pipe = new CreateColorPipe();
    expect(pipe).toBeTruthy();
  });
});
